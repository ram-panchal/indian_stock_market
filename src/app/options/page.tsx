import { OptionChainView } from "@/components/options/option-chain-view";

export const metadata = { title: "Option chain — Indian Market" };

export default function OptionsPage() {
  return <OptionChainView />;
}
